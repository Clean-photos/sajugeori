-- 019: 잘못 저장된 생년월일(음력을 양력 칸에 입력) 자체 교정 배너용 컬럼.
--
-- 배경: §1(양력·음력 선택)이 붙기 전에는 입력 폼에 역법 선택지가 없었다.
-- 음력 생일을 쓰는 사람은 어쩔 수 없이 그 날짜를 양력 칸에 그대로 넣었을 것이고,
-- calendar='solar'로 저장된 그 값이 실제로는 음력 날짜라는 뜻이다. 이런 행은
-- 데이터만 봐서는 "잘못 입력된 것"과 "실제 양력인 것"을 구분할 수 없다(CEO
-- 확인, B안 채택) — 자동 마이그레이션이 불가능하므로, 본인에게 확인시켜
-- 스스로 고치게 유도하는 배너를 띄우고, 그 배너를 한 번 확인하면 다시 뜨지
-- 않게 이 컬럼에 기록한다.
--
-- NULL = 아직 확인 안 함(배너 노출 대상). 값이 있으면 확인 완료 시각.
ALTER TABLE saju_profiles
  ADD COLUMN IF NOT EXISTS birth_date_confirmed_at TIMESTAMPTZ;
